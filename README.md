# SDN_mininet_project
This project implements a Software Defined Networking (SDN) based system to simulate controlled packet loss in a network. Using the POX controller and Mininet emulator, the system introduces random packet drops for a specific flow, allowing analysis of network behavior under unreliable conditions.
